# nodejs-api

A simple Node.js REST API built with Express.

## Setup

```bash
npm install
node index.js
```

## Endpoints

### GET /health

Returns server health status.

**Response:**
```json
{ "status": "OK" }
```

Server runs on **port 8080**.
